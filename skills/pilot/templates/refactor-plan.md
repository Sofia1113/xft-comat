# 重构方案：{{topic}}

- 日期：{{date}}
- 工作流模式：`{{mode}}`

## 行为基线

初始化后改写：记录重构前必须保持不变的用户可见行为和测试基线。

## 重构目标

初始化后改写：说明结构改善目标，不写新增功能。

## 不变约束

- 对外行为：初始化后改写
- API 合约：初始化后改写
- 数据结构：初始化后改写

## 变更步骤

初始化后改写：列出安全重构顺序。

## 回滚策略

初始化后改写：说明失败时如何回退或缩小改动。
