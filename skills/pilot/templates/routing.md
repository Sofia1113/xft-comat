# 路由记录：{{topic}}

- 日期：{{date}}
- 推荐工作流：`{{mode}}`
- 任务摘要：{{summary}}

## Provisional route

初始化后改写：记录开始时的临时路由假设。临时路由只用于决定探索和澄清深度，不能当作最终结论，也不能省略后续 final route。

- 临时类型：初始化后改写
- 临时复杂度评分：初始化后改写（仅 feature 需要；bugfix/refactor 写“类型优先，不按 feature 分数定档”）
- 临时理由：初始化后改写
- 需要澄清后确认的风险：初始化后改写

## 澄清对路由的影响

初始化后改写：逐项记录关键澄清如何改变或确认了任务类型、复杂度评分、风险等级和验收范围；若无需澄清，写明“需求清晰，未发现会影响路由的澄清项”，并说明依据。

## Final route

初始化后改写：澄清足够后填写最终路由。final route 必须能被任务类型、复杂度维度和风险证据支撑。

- 类型判断：初始化后改写
- 复杂度评分：初始化后改写（列出命中的维度和总分；bugfix/refactor 写“不适用，类型优先”）
- 命中信号：初始化后改写（来自用户需求、代码探索或现有失败证据）
- 最终选择：`{{mode}}`
- 选择理由：初始化后改写

## 路由修正

若 final route 与 provisional route 不同，记录修正后的模式、触发修正的事实和影响；若没有变化，说明澄清或代码探索如何确认了初始判断。
