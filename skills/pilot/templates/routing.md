# 路由记录：{{topic}}

- 日期：{{date}}
- 推荐工作流：`{{mode}}`
- 任务摘要：{{summary}}

## Provisional route

初始化后改写：记录开始时的临时路由假设，只能用于决定澄清深度，不能当作最终结论。

- 临时类型：初始化后改写
- 临时复杂度评分：初始化后改写
- 临时理由：初始化后改写
- 需要澄清后确认的风险：初始化后改写

## 澄清对路由的影响

初始化后改写：逐项记录哪些用户回答改变或确认了任务类型、复杂度评分、风险等级和验收范围。

## Final route

初始化后改写：澄清足够后填写最终路由。

- 类型判断：初始化后改写
- 复杂度评分：初始化后改写
- 命中信号：初始化后改写
- 最终选择：`{{mode}}`
- 选择理由：初始化后改写

## 路由修正

若 final route 与 provisional route 不同，在这里记录修正后的模式和原因；若没有变化，说明澄清如何确认了初始判断。
