# 设计方案：{{topic}}

- 日期：{{date}}
- 工作流模式：`{{mode}}`

## 用户最终抉择

初始化后改写：`feature-hard` 必须先记录用户在方案比较后的选择、取舍和约束，再写下方推荐方案；没有用户确认前写“等待用户抉择，阻塞原因：...”，不得伪装成已定稿。`feature-medium` 若无需用户关键抉择，写明“本任务无需用户关键抉择，因为...”。

## 方案目标

初始化后改写：说明本方案要解决的目标、非目标和成功标准；避免把实现步骤写成目标。

## 推荐方案

- 设计：初始化后改写
- 理由：初始化后改写（说明为什么符合当前代码结构和需求约束）
- 风险：初始化后改写（列出风险、触发条件和缓解方式）
- 测试策略：初始化后改写（对应验收标准和主要回归风险）

## 备选方案（仅在存在真实架构取舍时填写）

若不存在真实架构取舍，整节写明“无备选方案（不存在真实架构取舍）”即可；存在取舍时按下列各项填写，且每个备选必须能解释为什么没有被选中：

- 设计：
- 适用条件：
- 不推荐原因或权衡：

## 实现边界

初始化后改写：列出允许改动、禁止改动、兼容性约束和回滚边界。

## 验证策略

初始化后改写：只列与当前任务风险直接相关的验证。若包含 UI、浏览器流程、表单、导航、可视状态或权限页面，E2E 必须使用 `agent-browser`，且发生在代码审查修复之后。
