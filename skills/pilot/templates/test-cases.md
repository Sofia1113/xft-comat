# 测试用例 Round {{round}}：{{topic}}

- 日期：{{date}}
- 工作流模式：`{{mode}}`
- 任务摘要：{{summary}}
- 本轮原因：{{reason}}

## 测试矩阵

只列出与本任务风险直接相关的测试；不要为了填满模板而创建无关测试。

初始化后改写：列出真实测试用例、类型、目标、执行状态和证据；交付前不得保留本行。

建议格式（初始状态固定写 `待验证`，跑测试后用 `check-test` 命令就地更新为 `通过`/`未通过`，不要手改状态词）：

- [ ] TC-001 — 待验证 — 类型：unit/integration/e2e/regression/equivalence — 目标：待补充 — 证据：执行后补入证据

## UI / E2E 要求

如任务包含 UI、浏览器流程、登录、表单、导航、可视状态、权限页面或桌面 WebView，必须使用 `agent-browser`，并在 skill 使用记录中记录为 `required` 且补入真实操作证据。E2E 必须发生在代码审查和审查修复之后；若审查后影响用户路径，必须重新执行受影响路径。

## 失败记录

记录本轮失败、证据和修复后重验结果。只有当测试范围、验收标准或失败假设变化时，才创建新的 round 文档。
