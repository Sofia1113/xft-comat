# 设计说明：{{topic}}

- 日期：{{date}}
- 工作流模式：`{{mode}}`

## 实现策略

初始化后改写：说明最小实现路径、复用的现有模式，以及不引入额外抽象的理由；如确需新增抽象，说明它消除的具体复杂度。

## 影响范围

初始化后改写：列出预计改动文件、模块、数据流或用户可见行为；明确哪些相邻模块不应被改动。

## 测试策略

初始化后改写：列出与风险对应的最小测试和证据来源；若不补自动化测试，必须说明风险为什么可接受以及替代验证是什么。

## 风险

初始化后改写：列出已知风险、触发条件和缓解方式；若没有发现，写“未发现额外风险”。
