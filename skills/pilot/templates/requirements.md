# 用户需求：{{topic}}

- 日期：{{date}}
- 工作流模式：`{{mode}}`
- 任务摘要：{{summary}}

## 已确认目标

初始化后改写：列出用户真正要达成的目标，优先写可观察结果；不要复述实现手段当目标。

## 非目标

初始化后改写：列出明确不做的范围和容易误扩大的边界；若无非目标，写“本轮未确认额外非目标”。

## 业务规则

初始化后改写：列出影响设计、实现和验收的业务规则；若任务没有业务规则，写明“未发现业务规则，仅按现有代码约定处理”。

## 边界条件

初始化后改写：列出权限、数据、环境、兼容性、规模、异常路径和回退行为等边界；未知项必须写为假设或阻塞，不能隐含默认值。

## 验收标准

初始化后改写：列出可验证的验收标准。每条标准都应能对应到测试、命令、浏览器操作、截图或人工检查证据。

## 澄清记录

初始化后改写：逐轮记录关键追问和用户回答。每轮必须说明问题为什么重要、用户如何回答、哪些回答影响了路由、设计或验收；澄清由主会话主持，若引入 specialist 做缺口清单或需求归档审查，记录其结论。若需求本身已足够清楚、无澄清轮次，写明“需求清晰，无澄清轮次”，并补一句判断依据。
