# 实现任务列表：{{topic}}

- 日期：{{date}}
- 工作流模式：`{{mode}}`

## 任务

<!-- 本节由 add-task / set-task 命令程序维护并整体渲染，不要手改本文档。init 后即为有效空态；feature-hard 必须用 add-task 登记结构化任务。实现、测试、前端落地、审查修复类任务的 owner 是 worker（缺省值），不得是主会话；编排/澄清/拍板/核验类可标 --kind coordination。实现任务按「一个测试点 + 最小实现」拆分，标题写明涉及文件；任务间文件范围不相交是硬约束，重叠或共享测试入口的用 --deps 串联；无依赖且文件不相交的任务会被并发分派给多个 worker。一个内聚单元拆碎后仍要共享上下文时，宁可保留为一个稍大的任务。 -->

暂无任务，使用 add-task 登记任务拆分、owner 与状态。

## 完成定义

- 需求、设计或修复策略已覆盖任务边界。
- 测试先行或补齐回归测试；无法自动化时记录替代验证。
- 实现符合已确认需求和设计。
- 相关测试通过并有证据。
- 如有 UI，审查修复后完成 E2E 验证。
- 代码审查问题已处理或记录为残余风险。
