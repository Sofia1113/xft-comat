# 设计方案：{{topic}}

- 日期：{{date}}
- 工作流模式：`{{mode}}`

## 用户最终抉择

<!-- XFT-TODO: feature-hard 由主会话在 decide 阶段经 record-decision 写入用户的选择、取舍和约束；架构师不要替用户填写本节，未拍板前保留本哨兵。feature-medium 若无需用户关键抉择，写明"本任务无需用户关键抉择，因为..."。 -->

## 方案目标

<!-- XFT-TODO: 说明本方案要解决的目标、非目标和成功标准；避免把实现步骤写成目标。 -->

## 推荐方案

<!-- XFT-TODO: 写明设计、理由（为什么符合当前代码结构和需求约束）、风险（触发条件和缓解方式）、测试策略（对应验收标准和主要回归风险）。 -->

## 待用户拍板的决策点

<!-- XFT-TODO: feature-hard 必须列出需要用户拍板的关键决策点（每个决策点附推荐选项与理由），decide 阶段由主会话逐个向用户确认；无需用户拍板时写明"无关键决策点，因为..."。 -->

## 备选方案（仅在存在真实架构取舍时填写）

<!-- 若不存在真实架构取舍，整节写明"无备选方案（不存在真实架构取舍）"；存在取舍时写明设计、适用条件、不推荐原因或权衡，每个备选必须能解释为什么没有被选中。 -->

## 实现边界

<!-- XFT-TODO: 列出允许改动、禁止改动、兼容性约束和回滚边界。 -->

## 验证策略

<!-- XFT-TODO: 只列与当前任务风险直接相关的验证。若包含 UI、浏览器流程、表单、导航、可视状态或权限页面，E2E 必须使用 agent-browser，且发生在代码审查修复之后。 -->
